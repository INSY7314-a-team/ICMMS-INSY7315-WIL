@model ICCMS_Web.Models.DashboardViewModel
@using System.Security.Claims
@inject IConfiguration Configuration
@{
    ViewData["Title"] = "Project Manager – Quotes";
    var token = User.FindFirst("FirebaseToken")?.Value ?? string.Empty;
}

<style>
  .btn-brand{background:#FFD200;border-color:#FFD200;color:#111}
  .btn-brand:hover{filter:brightness(.95)}
  .quote-summary-sticky{position:sticky;bottom:0;background:#FFF7CC;border-top:1px solid #E6C200;padding:12px 8px;box-shadow:0 -2px 6px rgba(0,0,0,.04);z-index:1}
  .quote-summary-sticky .form-control{height:34px;padding:4px 8px}
</style>

<div class="container-fluid px-3 px-md-4 py-3">

  <!-- Header -->
  <div class="d-flex flex-wrap align-items-center justify-content-between mb-3">
    <div>
      <h1 class="h3 fw-bold mb-1">Project Manager</h1>
      <div class="text-muted">Quote generation + snapshot</div>
    </div>
    <div class="d-flex gap-2">
      <a class="btn btn-outline-secondary" asp-controller="Quotes" asp-action="Index">
        <i class="fas fa-list"></i> View All Quotes
      </a>
      <button class="btn btn-brand" id="btn-new-quote">
        <i class="fas fa-file-invoice"></i> Generate New Quote
      </button>
    </div>
  </div>

  <!-- Quotes Snapshot Card -->
  <div class="card shadow-sm mb-4">
    <div class="card-header bg-light d-flex justify-content-between align-items-center">
      <h5 class="mb-0">Quotes Snapshot</h5>
      <span class="text-muted small">Total Quotes: <strong>@Model?.TotalQuotes</strong></span>
    </div>
    <div class="card-body">
      <h6 class="mb-2">Recently Accepted</h6>
      <ul class="list-group mb-3">
        @if (Model?.RecentAcceptedQuotes != null && Model.RecentAcceptedQuotes.Any())
        {
            foreach (var q in Model.RecentAcceptedQuotes)
            {
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <div>
                        <div><strong>@q.Title</strong> <span class="badge bg-success ms-2">Accepted</span></div>
                        <div class="text-muted small">
                            @if (q.DecidedAt.HasValue)
                            {
                                @q.DecidedAt.Value.ToString("yyyy-MM-dd HH:mm")
                            }
                            else
                            {
                                @:(no decided date)
                            }
                        </div>
                        <div class="text-muted small">@q.ClientName</div>
                    </div>
                    <a asp-controller="Invoices" asp-action="Create" asp-route-quoteId="@q.Id"
                       class="btn btn-sm btn-success">
                        Create Invoice & Start Project
                    </a>
                </li>
            }
        }
        else
        {
            <li class="list-group-item text-muted">No accepted quotes yet.</li>
        }
      </ul>
      <a class="btn btn-outline-primary btn-sm" asp-controller="Quotes" asp-action="Index">Go to Quotes</a>
    </div>
  </div>

</div>

<!-- NEW QUOTE WIZARD (simplified: Client → Blueprint → Estimate → Preview) -->
@await Html.PartialAsync("~/Views/Shared/_QuoteWizard.cshtml")


<!-- Hidden Preview POST form -->
<form id="quote-preview-form" method="post" asp-controller="Quotes" asp-action="Preview" target="_blank" class="d-none">
  @Html.AntiForgeryToken()
</form>

<!-- Boot flags for JS -->
<script>window.PM = { token: '@token', root: '@Url.Content("~/")', QUOTE_ONLY: true };</script>
<!-- <script src="~/js/pm/pm-quote.js"></script>-->
